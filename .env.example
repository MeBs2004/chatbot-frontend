# Nuformly public widget frontend — environment variables.
# Copy to .env and fill in real values. Never commit .env.

# Base URL of the backend's public API (e.g. https://api.yourdomain.com/)
VITE_BACKEND_URL=http://localhost:4002/
